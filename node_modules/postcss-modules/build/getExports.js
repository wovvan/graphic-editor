'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = getExports;
function getExports(css) {
  var exportRules = [];
  var exports = {};

  css.each(function (exportRule) {
    if (exportRule.selector === ':export') exportRules.push(exportRule);
  });

  var lastExport = exportRules.pop();
  if (lastExport) lastExport.walkDecls(function (decl) {
    return exports[decl.prop] = decl.value;
  });

  return exports;
}